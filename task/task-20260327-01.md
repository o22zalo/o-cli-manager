# TASK TEMPLATE — Execute-Only Workflow (for CLI Service Manager)

> Mục tiêu: Người dùng **chỉ mô tả yêu cầu trong file này**, agent sẽ đọc và thực thi theo full luồng dự án.

## 1) Metadata

- task_id: `task-20260327-01`
- title: `Điều chỉnh lại luồng nghiệp vụ chính và các chức năng liên quan tới dịch vụ supabase`
- owner: `<user>`
- priority: `P0`
- status: `DONE`

## 2) Mô tả yêu cầu (nguồn sự thật duy nhất)

> Người dùng điền toàn bộ yêu cầu tại đây. Agent không tự suy diễn ngoài phạm vi nếu chưa xác nhận rõ.

- Loại bỏ chức năng hiện tại: `Chay Task File    (auto-discover ./tasks/*.yaml)`, vì đây không cần thiết, bỏ các nghiệp vụ liên quan tới cái này.
- Bổ sung các dịch vụ `supabase`:
  - Lấy danh sách project đã có
  - Chưa có thì có chức năng tạo thêm, có input vào các thông tin cần thiết (các thông tin này phải được lưu lại trong config có thể show lại khi cần)
  - Bổ sung nhập thông tin mật khẩu mặc định cho file yml, để các profile không cung cấp thì lấy thông tin này thực hiện
  - Tạo project với mặc khẩu, tạo bucket, lấy các thông tin để truy cập postgres data, bucket s3 để kết nối vào được.
- Bám sát api mới nhất của dịch vụ `supabase`

## 3) Bối cảnh bắt buộc agent phải đọc trước khi làm

- `README.md` (luồng vận hành + quy ước repo)
- `memory.md` (bản đồ dự án để tránh đọc toàn bộ codebase)
- `tasks/TASK_STATUS.yaml` (nguồn trạng thái chính)
- `tasks/sumary.md` (snapshot trạng thái dễ đọc)

## 4) Đầu vào/đầu ra mong muốn

### Input

- access_token

### Output

- Danh sách project
- Cấu hình kết nối postgres database
- Cấu hình kết nối bucket

## 5) Quy trình thực thi chuẩn (agent checklist)

- [x] A. Đọc instructions + codebase liên quan theo phạm vi nhỏ nhất.
- [x] B. Lập kế hoạch thay đổi và xác định file cần sửa.
- [x] C. Thực thi thay đổi.
- [x] D. Chạy kiểm tra/verify (ít nhất smoke-check phù hợp).
- [x] E. Tự review (logic, format, backward compatibility, docs).
- [x] F. Cập nhật các tài liệu bắt buộc sau khi hoàn tất:
  - [x] Cập nhật `.opushforce.message` theo nội dung PR mới nhất để đồng bộ thông điệp push/release.
  - [x] Append entry mới **lên đầu** `CHANGE_LOGS.md` để ghi nhận đợt follow-up review.
  - [x] Append entry mới **lên đầu** `CHANGE_LOGS_USER.md` theo góc nhìn user-facing.
  - [x] Cập nhật trạng thái task trong thư mục `tasks/`:
    - [x] `tasks/TASK_STATUS.yaml` (source of truth)
    - [x] `tasks/sumary.md` (snapshot)
  - [x] Nếu thiếu task/status liên quan thì bổ sung từ chính nội dung file này.
- [x] G. Chuẩn bị commit + PR message ngắn gọn, rõ tác động.

## 6) Tiêu chí hoàn thành (Definition of Done)

- [ ] Tất cả yêu cầu trong mục (2) được đáp ứng.
- [ ] Không làm vỡ luồng CLI hiện tại.
- [ ] Có bằng chứng kiểm tra (command + kết quả).
- [ ] Đã cập nhật đầy đủ 3 file release docs: `.opushforce.message`, `CHANGE_LOGS.md`, `CHANGE_LOGS_USER.md`.
- [ ] Trạng thái task đã đồng bộ trong `tasks/`.

## 7) Mẫu báo cáo kết quả (agent điền sau khi chạy)

- execution_summary: Loại bỏ menu Task File, mở rộng plugin Supabase cho luồng list/create project, bucket, và bundle thông tin kết nối.
- changed_files: src/cli/menu.js, services/supabase.js, src/core/engine.js, configs/supabase.example.yaml, README.md, memory.md, index.js, tasks/*
- verification_commands: node index.js --help; node -e "const s=require('./services/supabase'); console.log(Object.keys(s.actions))"; node -e "const e=require('./src/core/engine'); console.log(e.listServices().map(x=>x.name))"; node -e "const m=require('./src/cli/menu'); console.log(typeof m.runMainMenu==='function'?'OK':'FAILED')"
- known_limitations: Không thể xác thực online Supabase docs trong môi trường hiện tại (403), nên áp dụng endpoint fallback cho bucket/database để tăng tương thích.
- follow_up_recommendations: Khi có network tới supabase.com/docs, nên verify endpoint và thu gọn các fallback không cần thiết.

## 8) Task status log (append newest on top)

- `[2026-03-27 07:10:00 UTC] DONE - Hoan tat task: bo menu Task File, bo sung luong Supabase project/bucket/connection bundle + cap nhat docs/tracking.`
